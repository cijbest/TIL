package test;

import java.util.*;

class Node{
	int x, y;
	Node(int x, int y){
		this.x = x;
		this.y = y;
	}
}
public class Test_HashMap {

		Map<Node, Integer> map = new HashMap<>();
//		map.put(new Node(5,1), 2);
//		map.put(new Node(2,7), 2);
//		map.put(new Node(2,3), 2);
//		map.put(new Node(4,2), 3);
//		map.put(new Node(4,1), 3);
//		List<Map.Entry<Node, Integer>> entryList = new LinkedList<>(map.entrySet());
//		entryList.sort((o1, o2) -> o2.getValue() - o1.getValue());
//		for(Map.Entry<Node, Integer> entry : entryList){
//		    System.out.println("key : (" + entry.getKey().x + ", " + entry.getKey().y  + "), value : " + entry.getValue());
//		}

//	}

}
